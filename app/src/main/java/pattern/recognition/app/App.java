/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package pattern.recognition.app;

import pattern.recognition.list.LinkedList;

import static pattern.recognition.utilities.StringUtils.join;
import static pattern.recognition.utilities.StringUtils.split;
import static pattern.recognition.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
